version: 2.1
commands:
  hello:
    description: "Say hello"
    steps:
      - run:
          name: "Say hello"
          command: echo "Hello"

jobs:
  build:
    docker:
      - image: cimg/node:18.4.0
    steps:
      - checkout
      - hello

workflows:
  build:
    jobs:
      - build:
          filters:
            branches:
              only: /^main$/



